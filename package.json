{
  "name": "financas-pj-root",
  "version": "1.0.0",
  "description": "Root de gerenciamento do projeto de finanças PJ",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install-all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
